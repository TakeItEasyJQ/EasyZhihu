<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.easyzhihu">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="EasyZhihu"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:label">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.ActivityContent" />
        <activity android:name=".Activities.ThemeContentActivity" />
        <activity android:name=".Activities.EditorActivity" />
        <activity android:name=".Activities.EditorHomePageActivity" />
        <activity android:name=".Activities.MessageActivity" />
        <activity android:name=".Activities.StoryFavorActivity" />
        <activity android:name=".Activities.SectionActivity"></activity>
    </application>

</manifest>